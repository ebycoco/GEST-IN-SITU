%APPDATA%

[SKILL.md](file;file:///d%3A/Espace%20travail/GEST_IN-SITU_CARTE_ABOBO_V2/.agents/skills/agent_0_orchestrator_master/SKILL.md) 
*********************************************************************************************

3. **Validation Statique, Confinement & Documentation (Gouvernance TITAN) :**
### 🛡️ Directives de Confinement & Sécurité (Mode Production)
- **Principe STOP & WARN :** Si la correction nécessite de modifier un composant global de navigation, préviens-moi avant d'appliquer le changement.
   - **Confinement :** Inspection en lecture seule.
   - **Validation Statique :** Exécuter `npx tsc --noEmit` (0 erreur).
   - **Mise à jour de la Mémoire & Documentation :** 
     - Consigner les conclusions de l'audit de volume dans `.agents/storage/factory_memory.md`.
     - **Mettre OBLIGATOIREMENT à jour le fichier `.agents/storage/GEMINI.md`** pour documenter le comportement du pré-scan CSV par rapport aux insertions réelles.

   - Aucun `npm run build` ne doit être exécuté.

---

### 📢 Restitution
Présente le compte-rendu final selon la Norme des 13 Agents TITAN.
***********************************************************************************************************
Agent, je lance une mission d'audit technique complet du module [INSÉRER LE NOM DE LA PAGE ICI, ex: Administrateur du Site]. Je veux que tu appliques une rigueur totale sur cette partie du projet.

1. Scan Exhaustif :

Scanne tout le code source lié à ce module (Front-end, Back-end, requêtes SQL/IPC).

Examine chaque fonctionnalité, bouton, et interaction utilisateur.

2. Analyse de Risque :

Logique Métier : Y a-t-il des failles ? Les règles de gestion sont-elles respectées ?

Stabilité & Performance : Y a-t-il des fuites de mémoire, des requêtes SQL non optimisées ou des risques de plantage sur gros volume ?

Flux de Données : Chaque action de modification ou création respecte-t-elle notre règle unifiée (is_dirty = 1 + Audit Log + Transaction SQLite) ?

Sécurité : Les accès sont-ils bien sécurisés par l'identité de l'agent connecté ? (Pas de privilèges excessifs, pas d'utilisation de comptes 'Maître' par défaut).

3. Livrable attendu :

Rapport d'Audit Complet : Identifie les problèmes (bugs, lenteurs, erreurs de logique, failles de sécurité).

Plan de Remédiation : Propose un plan d'action structuré, étape par étape, pour corriger chaque point. Priorise par criticité.

Consigne Finale :

Sois impitoyable sur la qualité du code.

Attends impérativement ma validation sur le 'Plan de Remédiation' avant de procéder à la moindre modification. Je veux relire ton plan pour m'assurer qu'il est parfait.


*************************************************************************************************************************************************

Agent, nous lançons une mission d'audit de sécurité sur le Cloisonnement Supabase RLS (Row Level Security) et la Protection des Secrets Client.

Tu dois analyser le code et la configuration du projet pour vérifier les points suivants :

1. Audit des Clés Supabase
Confirme que la clé SUPABASE_SERVICE_ROLE_KEY n'est jamais importée ni présente dans le code distribué client/Electron.

Seule la clé SUPABASE_ANON_KEY doit être utilisée par le client.

2. Audit de la Sécurité des Sessions Locaux
Vérifie comment est stocké le token de session ou les identifiants utilisateur sur le disque local (fichiers JSON, localStorage, SQLite).

S'ils sont en clair, propose l'intégration de l'API native Electron safeStorage pour chiffrer ces données via DPAPI / OS Keychain.

3. Script d'Audit des Politiques RLS Supabase
Génère un script d'audit SQL que nous pourrons exécuter sur la console Supabase pour vérifier que les tables (t_cartes, t_users, etc.) possèdent bien des politiques ROW LEVEL SECURITY restreignant la lecture/écriture par site_id.

Consigne : Analyse le code, donne-moi le diagnostic et génère le script SQL d'audit RLS. Compile avec npx tsc --noEmit